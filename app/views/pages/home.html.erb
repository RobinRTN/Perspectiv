<% if user_signed_in? %>
  <div class="bg-home">
    <div class="banner" id="banner-logged">
      <%= image_tag "logo-big.png", class: "logo-big"%>
      <h2> 👋 Bienvenue <%= current_user.first_name %></h2>
    </div>
    <div class="container">
      <div class="section">
        <div class="d-flex justify-content-between mb-3 headers-section">
          <h3>Les mentors à découvrir</h3>
          <%= link_to "Voir plus >", mentors_path, class:"link-purple"%>
        </div>
        <div class="scroll-horizontal">
          <% @custom_mentors.each do |mentor| %>
            <%= render "shared/item_scroll_mentor", mentor: mentor %>
          <% end %>
        </div>
      </div>
      <div class="section">
        <div class="d-flex justify-content-between mb-3 headers-section">
          <h3>Notre sélection de vidéos pour toi</h3>
          <%= link_to "Voir plus >", mentors_path, class:"link-purple"%>
        </div>
        <!-- Slider main container -->
        <div class="swiper" data-controller="swiper">
          <!-- Additional required wrapper -->
          <div class="swiper-wrapper">
            <!-- Slides -->
            <% @custom_contents.each do |content| %>
              <div class="swiper-slide">
                <%= render "shared/item_scroll_video", content: content %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

<% else %>
  <div class="bg-landing">
    <div class="container">
      <div class="home-banner-left" id="first">
        <div class="home-banner-text" id="left">
          <%= image_tag "logo-big.png", class: "logo-big"%>
          <h5>Trouve ta voie, éclaire ta perspective</h5>
          <%= link_to 'Réponds au quiz', new_user_registration_path, class: "btn-call-to-action" %>
        </div>
        <div class="home-banner-spline">
          <iframe src='https://my.spline.design/miniroomartcopy-4db798195503a8e953ea923dec73af01/' frameborder='0' width='100%' height='100%'></iframe>
          <div class="purple-box"></div>
        </div>
      </div>

      <div class="home-banner-right">
        <div class="home-banner-spline">
          <%= image_tag "girl_zoomed1.png", width:'100%', height:'100%' %>
        </div>
        <div class="home-banner-text" id="right">
          <h2 style="text-align: end;">Découvre nos mentors</h2>
          <h5 style="text-align: end;">Accède aux mentors qui te correspondent</h5>
        </div>
      </div>

      <div class="home-banner-left">
        <div class="home-banner-text" id="left">
          <h2>Rencontre nos mentors</h2>
          <h5>Réserve un entretien et obtiens des conseils</h5>
          <%= link_to 'Réponds au quiz', new_user_registration_path, class: "btn-call-to-action" %>
        </div>
        <div class="home-banner-spline">
          <%= image_tag "mentor_student.png", width:'100%', height:'100%' %>
        </div>
      </div>
    </div>
  </div>


<% end %>
